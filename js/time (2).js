(function attachTimeModule() {
  const Ello = (window.Ello = window.Ello || {});

  Ello.initTime = function initTime() {
    const mediaItems = document.querySelectorAll(".time__media");
    if (!mediaItems.length) {
      return;
    }
  };
})();
